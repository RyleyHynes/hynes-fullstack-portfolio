/**
 * Build-time constants injected by Vite's `define` config.
 * They are available globally in app code, so TypeScript needs ambient declarations here.
 */
declare const __APP_VERSION__: string
declare const __APP_GIT_SHA__: string
